export enum ComplexityTypes {
    undefined = 0,
    easy = 1,
    medium = 2,
    hard = 3,
    nightmare = 4,
    info = 5,
}

export enum StudentTaskStatuses {
    none = 0,
    done = 1,
    selected = 2,
}

export enum LabTypes {
    individual = 0,
    common = 1,
}
